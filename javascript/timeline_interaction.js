Timeline.prototype.handleNodeClick = function(e) {
	var nodeId = e.closest(".marker").attr("id");
	var nodeInd = parseInt(nodeId.substring(("marker").length));
	if (this.eventObj === undefined) {
		$("")
	}
	else {


	}
}